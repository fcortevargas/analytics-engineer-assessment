version: 2

models:
  - name: dim_currency
    columns:
      - name: currency_key
        description: The surrogate key of the currency.
        tests:
          - not_null
          - unique
      - name: currency_code
        description: The three-letter code of the currency.
        tests:
          - not_null
          - unique